import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab2Detail3Page } from './tab2-detail3.page';

describe('Tab2Detail3Page', () => {
  let component: Tab2Detail3Page;
  let fixture: ComponentFixture<Tab2Detail3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab2Detail3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
