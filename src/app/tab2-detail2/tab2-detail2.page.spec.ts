import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab2Detail2Page } from './tab2-detail2.page';

describe('Tab2Detail2Page', () => {
  let component: Tab2Detail2Page;
  let fixture: ComponentFixture<Tab2Detail2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab2Detail2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
