import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab2Detail4Page } from './tab2-detail4.page';

describe('Tab2Detail4Page', () => {
  let component: Tab2Detail4Page;
  let fixture: ComponentFixture<Tab2Detail4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab2Detail4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
